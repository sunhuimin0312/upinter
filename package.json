{
  "name": "xhs-ops-dashboard",
  "version": "1.0.0",
  "description": "샤오홍슈 운영 대시보드 - UP INTER",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3000",
    "start": "npx serve . -l 3000",
    "preview": "npx serve . -l 3000",
    "deploy": "npx wrangler pages deploy . --project-name=xhs-ops-dashboard"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
